import { describe, expect, it } from 'vitest'

describe('editor-ui dummy test', () => {
  it('passes for now', () => {
    // Arrange
    const value = true

    // Act
    const result = value

    // Assert
    expect(result).toBe(true)
  })
})

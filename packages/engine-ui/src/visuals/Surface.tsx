export function Surface(): React.JSX.Element {
  return <div>Surface</div>
}

/*

import {
  ISurfaceDefinitionProvider,
  surfaceDefinitionProviderToken,
} from '@ancadeba/engine'
import { useService } from '@ancadeba/ui'

export function Surface(): React.JSX.Element {
  const surfaceProvider = useService<ISurfaceDefinitionProvider>(
    surfaceDefinitionProviderToken,
  )
  return <div>Surface</div>
}

*/

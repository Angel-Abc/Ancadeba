# Agent instructions

# Additional instructions
The editor is a different kind of application and therefor should not try to use the classes and components of the engine.
The only code that can be shared between the game engine and the editor is code from @utils, @ioc and @loader/schema
